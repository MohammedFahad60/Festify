import {describe,expect,it,vi,afterEach} from "vitest";import {api} from "./api";
afterEach(()=>vi.restoreAllMocks());
describe("frontend API client",()=>{it("uses relative URLs and credentials",async()=>{const fetch=vi.spyOn(globalThis,"fetch").mockResolvedValue(new Response(JSON.stringify({success:true,data:{ok:true}}),{status:200,headers:{"content-type":"application/json"}}));const result=await api.get<{ok:boolean}>("/api/v1/health");expect(fetch).toHaveBeenCalledWith("/api/v1/health",expect.objectContaining({credentials:"include",method:"GET"}));expect(result.success).toBe(true);});it("refreshes once on 401 and retries",async()=>{const fetch=vi.spyOn(globalThis,"fetch").mockResolvedValueOnce(new Response(JSON.stringify({success:false,error:{code:"UNAUTHENTICATED",message:"Authentication required"}}),{status:401})).mockResolvedValueOnce(new Response(JSON.stringify({success:true,data:null}),{status:200})).mockResolvedValueOnce(new Response(JSON.stringify({success:true,data:{id:"1"}}),{status:200}));const result=await api.get<{id:string}>("/api/v1/users/me");expect(fetch).toHaveBeenCalledTimes(3);expect(result.data?.id).toBe("1");});});
