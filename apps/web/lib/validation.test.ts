import {describe,expect,it} from "vitest";import {loginSchema,registerSchema} from "@festify/validation";describe("auth form contracts",()=>{it("rejects malformed login",()=>expect(loginSchema.safeParse({email:"bad",password:""}).success).toBe(false));it("requires a strong registration password",()=>expect(registerSchema.safeParse({name:"User",email:"user@example.com",password:"weak"}).success).toBe(false));it("accepts valid registration values",()=>expect(registerSchema.safeParse({name:"Example User",email:"USER@example.com",password:"SecureFestify123"}).success).toBe(true));});
